name: Validate crate

on:
  pull_request:
    types: [opened, reopened, edited, synchronize]
    branches: main

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: giancosta86/aurora-github/actions/validate-rust-crate@crates
